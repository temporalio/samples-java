// Generated by nex-gen. DO NOT EDIT.
package io.temporal.samples.nexuswalkthrough.generatedservice;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Objects;
import org.jspecify.annotations.Nullable;

@JsonSerialize(using = SubmitDecisionInput.Serializer.class)
@JsonDeserialize(using = SubmitDecisionInput.Deserializer.class)
public final class SubmitDecisionInput {
  public static final class Decision {
    public static final Decision DECISION_APPROVED = new Decision("APPROVED");
    public static final Decision DECISION_DENIED = new Decision("DENIED");

    private final String value;

    private Decision(String value) {
      this.value = value;
    }

    @JsonCreator
    public static @Nullable Decision fromString(String value) {
      if (value == null) {
        return null;
      }
      if ("APPROVED".equals(value)) {
        return DECISION_APPROVED;
      }
      if ("DENIED".equals(value)) {
        return DECISION_DENIED;
      }
      throw new IllegalArgumentException(
          "must be one of [\"APPROVED\", \"DENIED\"], got \"" + value + "\"");
    }

    @JsonValue
    public String getValue() {
      return value;
    }

    @Override
    public boolean equals(@Nullable Object other) {
      if (this == other) {
        return true;
      }
      if (!(other instanceof Decision)) {
        return false;
      }
      Decision that = (Decision) other;
      return Objects.equals(this.value, that.value);
    }

    @Override
    public int hashCode() {
      return Objects.hash(value);
    }

    @Override
    public String toString() {
      return "Decision[" + value + "]";
    }
  }

  private final String itemId;

  /** The decision being submitted. */
  private final Decision decision;

  public SubmitDecisionInput(String itemId, Decision decision) {
    this.itemId = itemId;
    this.decision = decision;
  }

  public String getItemId() {
    return itemId;
  }

  public Decision getDecision() {
    return decision;
  }

  @Override
  public boolean equals(@Nullable Object other) {
    if (this == other) {
      return true;
    }
    if (!(other instanceof SubmitDecisionInput)) {
      return false;
    }
    SubmitDecisionInput that = (SubmitDecisionInput) other;
    return Objects.equals(this.itemId, that.itemId) && Objects.equals(this.decision, that.decision);
  }

  @Override
  public int hashCode() {
    return Objects.hash(itemId, decision);
  }

  @Override
  public String toString() {
    return "SubmitDecisionInput{" + "itemId=" + itemId + ", decision=" + decision + "}";
  }

  public static final class Serializer
      extends com.fasterxml.jackson.databind.JsonSerializer<SubmitDecisionInput> {
    @Override
    public void serialize(
        SubmitDecisionInput value, JsonGenerator gen, SerializerProvider serializers)
        throws IOException {
      gen.writeStartObject();
      if (value.itemId != null) {
        gen.writeStringField("itemId", value.itemId);
      }
      if (value.decision != null) {
        gen.writeStringField("decision", value.decision.getValue());
      }
      gen.writeEndObject();
    }
  }

  public static final class Deserializer
      extends com.fasterxml.jackson.databind.JsonDeserializer<SubmitDecisionInput> {
    @Override
    public SubmitDecisionInput deserialize(JsonParser parser, DeserializationContext context)
        throws IOException {
      JsonNode node = parser.readValueAsTree();
      List<Violation> violations = new ArrayList<>();
      if (node == null || !node.isObject()) {
        violations.add(new Violation("", "expected object"));
        throw new ValidationException(violations);
      }
      Iterator<String> fieldNames = node.fieldNames();
      while (fieldNames.hasNext()) {
        String key = fieldNames.next();
        switch (key) {
          case "decision":
          case "itemId":
            break;
          default:
            violations.add(new Violation(key, "unknown field"));
        }
      }
      String itemId = null;
      {
        JsonNode field = node.get("itemId");
        if (field == null) {
          violations.add(new Violation("itemId", "required"));
        } else if (field.isNull()) {
          violations.add(new Violation("itemId", "explicit null not allowed"));
        } else {
          if (!field.isTextual()) {
            violations.add(new Violation("itemId", "expected string"));
          } else {
            itemId = field.textValue();
          }
        }
      }
      Decision decision = null;
      {
        JsonNode field = node.get("decision");
        if (field == null) {
          violations.add(new Violation("decision", "required"));
        } else if (field.isNull()) {
          violations.add(new Violation("decision", "explicit null not allowed"));
        } else {
          if (!field.isTextual()) {
            violations.add(new Violation("decision", "expected string"));
          } else {
            String decisionValue = field.textValue();
            if ("APPROVED".equals(decisionValue)) {
              decision = Decision.DECISION_APPROVED;
            } else if ("DENIED".equals(decisionValue)) {
              decision = Decision.DECISION_DENIED;
            } else {
              violations.add(
                  new Violation(
                      "decision",
                      "must be one of [\"APPROVED\", \"DENIED\"], got " + decisionValue));
            }
          }
        }
      }
      if (!violations.isEmpty()) {
        throw new ValidationException(violations);
      }
      return new SubmitDecisionInput(itemId, decision);
    }
  }
}
