// Generated by nex-gen. DO NOT EDIT.
package io.temporal.samples.nexuswalkthrough.generatedservice;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Objects;
import org.jspecify.annotations.Nullable;

@JsonSerialize(using = CheckApprovalRequiredInput.Serializer.class)
@JsonDeserialize(using = CheckApprovalRequiredInput.Deserializer.class)
public final class CheckApprovalRequiredInput {
  /** Identifier of the purchase. */
  private final String itemId;

  /** Who is asking. */
  private final String requester;

  /** Purchase amount. */
  private final double amount;

  public CheckApprovalRequiredInput(String itemId, String requester, double amount) {
    this.itemId = itemId;
    this.requester = requester;
    this.amount = amount;
  }

  public String getItemId() {
    return itemId;
  }

  public String getRequester() {
    return requester;
  }

  public double getAmount() {
    return amount;
  }

  @Override
  public boolean equals(@Nullable Object other) {
    if (this == other) {
      return true;
    }
    if (!(other instanceof CheckApprovalRequiredInput)) {
      return false;
    }
    CheckApprovalRequiredInput that = (CheckApprovalRequiredInput) other;
    return Objects.equals(this.itemId, that.itemId)
        && Objects.equals(this.requester, that.requester)
        && this.amount == that.amount;
  }

  @Override
  public int hashCode() {
    return Objects.hash(itemId, requester, amount);
  }

  @Override
  public String toString() {
    return "CheckApprovalRequiredInput{"
        + "itemId="
        + itemId
        + ", requester="
        + requester
        + ", amount="
        + amount
        + "}";
  }

  public static final class Serializer
      extends com.fasterxml.jackson.databind.JsonSerializer<CheckApprovalRequiredInput> {
    @Override
    public void serialize(
        CheckApprovalRequiredInput value, JsonGenerator gen, SerializerProvider serializers)
        throws IOException {
      gen.writeStartObject();
      if (value.itemId != null) {
        gen.writeStringField("itemId", value.itemId);
      }
      if (value.requester != null) {
        gen.writeStringField("requester", value.requester);
      }
      gen.writeNumberField("amount", value.amount);

      gen.writeEndObject();
    }
  }

  public static final class Deserializer
      extends com.fasterxml.jackson.databind.JsonDeserializer<CheckApprovalRequiredInput> {
    @Override
    public CheckApprovalRequiredInput deserialize(JsonParser parser, DeserializationContext context)
        throws IOException {
      JsonNode node = parser.readValueAsTree();
      List<Violation> violations = new ArrayList<>();
      if (node == null || !node.isObject()) {
        violations.add(new Violation("", "expected object"));
        throw new ValidationException(violations);
      }
      Iterator<String> fieldNames = node.fieldNames();
      while (fieldNames.hasNext()) {
        String key = fieldNames.next();
        switch (key) {
          case "amount":
          case "itemId":
          case "requester":
            break;
          default:
            violations.add(new Violation(key, "unknown field"));
        }
      }
      String itemId = null;
      {
        JsonNode field = node.get("itemId");
        if (field == null) {
          violations.add(new Violation("itemId", "required"));
        } else if (field.isNull()) {
          violations.add(new Violation("itemId", "explicit null not allowed"));
        } else {
          if (!field.isTextual()) {
            violations.add(new Violation("itemId", "expected string"));
          } else {
            itemId = field.textValue();
          }
        }
      }
      String requester = null;
      {
        JsonNode field = node.get("requester");
        if (field == null) {
          violations.add(new Violation("requester", "required"));
        } else if (field.isNull()) {
          violations.add(new Violation("requester", "explicit null not allowed"));
        } else {
          if (!field.isTextual()) {
            violations.add(new Violation("requester", "expected string"));
          } else {
            requester = field.textValue();
          }
        }
      }
      double amount = 0.0;
      {
        JsonNode field = node.get("amount");
        if (field == null) {
          violations.add(new Violation("amount", "required"));
        } else if (field.isNull()) {
          violations.add(new Violation("amount", "explicit null not allowed"));
        } else {
          if (!field.isNumber()) {
            violations.add(new Violation("amount", "expected number"));
          } else {
            amount = field.doubleValue();
          }
        }
      }
      if (!violations.isEmpty()) {
        throw new ValidationException(violations);
      }
      return new CheckApprovalRequiredInput(itemId, requester, amount);
    }
  }
}
