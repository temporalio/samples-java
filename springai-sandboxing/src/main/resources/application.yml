spring:
  application:
    name: spring-ai-temporal-sandboxing-example
  main:
    web-application-type: none
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7
  temporal:
    connection:
      target: localhost:7233
    workers:
      - task-queue: spring-ai-sandboxing-example
        workflow-classes:
          - io.temporal.springai.example.sandboxing.SandboxingWorkflowImpl

logging:
  level:
    io.temporal.springai: DEBUG
